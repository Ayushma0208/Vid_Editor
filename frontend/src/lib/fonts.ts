/** CSS class helpers — avoid next/font/google (Turbopack fetch bugs on Vercel). */
export const dmSans = { className: "font-dm-sans" }
export const syne = { className: "font-syne" }
export const jetBrainsMono = { className: "font-jetbrains" }
